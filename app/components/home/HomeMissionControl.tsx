"use client"
import type { ReactNode } from "react"
import { ArrowRight, CalendarDays, CheckCircle2, ClipboardCheck, Dumbbell, MessageSquare, Sparkles, Trophy, Users } from "lucide-react"
import TeamContextHeader from "../layout/TeamContextHeader"
import type { WorkspaceTab } from "../../lib/workspaces"
import { getActiveU11Players, getContinuingTeamTbcPlayers, leonardStanleyEvents } from "../../lib/realTeamData"
type Props={onNavigate:(tab:WorkspaceTab)=>void}
const players=getActiveU11Players(), continuingTbc=getContinuingTeamTbcPlayers(), nextEvent=leonardStanleyEvents[0]
export default function HomeMissionControl({onNavigate}:Props){const hour=new Date().getHours(),greeting=hour<12?"Good morning":hour<18?"Good afternoon":"Good evening",today=new Date().toLocaleDateString("en-GB",{weekday:"long",day:"numeric",month:"long"});const actions=[{title:"Prepare the next session",detail:`${nextEvent.title} is the next recorded team event. Open Training to prepare the plan and attendance.`,cta:"Prepare session",tab:"training" as WorkspaceTab,priority:"NOW"},...(continuingTbc.length?[{title:"Resolve squad decision",detail:`${continuingTbc.length} continuing player still needs a final team decision.`,cta:"Review players",tab:"players" as WorkspaceTab,priority:"REVIEW"}]:[]),{title:"Matchday ready",detail:"Open the live command centre before kick-off to set the squad, record events and manage changes.",cta:"Open Matchday",tab:"matchday" as WorkspaceTab,priority:"READY"}];return <div className="fos-mission-control"><section className="fos-mission-hero"><div className="fos-mission-glow"/><div className="fos-mission-top"><span><Sparkles size={13}/> COACH COMMAND</span><b>PRIVATE ALPHA</b></div><div className="fos-mission-date">{today}</div><h1>{greeting}, Mike.</h1><p>Your team, your next decisions and the fastest route to getting them done.</p><button className="fos-next-event" type="button" onClick={()=>onNavigate("training")}><span><small>NEXT UP</small><strong>{nextEvent.title}</strong><em>{nextEvent.dateLabel} · {nextEvent.timeLabel??"TBC"}{nextEvent.location?` · ${nextEvent.location}`:""}</em></span><ArrowRight size={19}/></button><div className="fos-kpis"><Kpi value={String(players.length)} label="Squad ready"/><Kpi value={String(continuingTbc.length)} label="Decisions"/><Kpi value="0" label="Season matches"/><Kpi value="1" label="Next event"/></div></section><TeamContextHeader currentSection="Home" nextEventLabel={`${nextEvent.title}: ${nextEvent.dateLabel}`}/><section className="fos-priority-panel"><div className="fos-section-heading"><div><small>TODAY</small><h2>Coach action centre</h2><p>Football OS surfaces useful next actions instead of making you hunt through menus.</p></div><span>{actions.length} actions</span></div><div className="fos-action-stack">{actions.map((a,i)=><button key={a.title} type="button" className="fos-priority-action" onClick={()=>onNavigate(a.tab)}><b>{String(i+1).padStart(2,"0")}</b><span><small>{a.priority}</small><strong>{a.title}</strong><em>{a.detail}</em></span><i>{a.cta}<ArrowRight size={15}/></i></button>)}</div></section><section className="fos-tools-panel"><div className="fos-section-heading"><div><small>FAST LANE</small><h2>One-tap tools</h2></div><span>6 tools</span></div><div className="fos-tool-grid"><Tool label="Matchday" note="Live command" icon={<Trophy/>} tab="matchday" go={onNavigate}/><Tool label="Training" note="Plan session" icon={<Dumbbell/>} tab="training" go={onNavigate}/><Tool label="Players" note="Squad profiles" icon={<Users/>} tab="players" go={onNavigate}/><Tool label="Attendance" note="Take register" icon={<ClipboardCheck/>} tab="training" go={onNavigate}/><Tool label="Calendar" note="Club events" icon={<CalendarDays/>} tab="club" go={onNavigate}/><Tool label="Parents" note="Communications" icon={<MessageSquare/>} tab="club" go={onNavigate}/></div></section><section className="fos-trust-strip"><CheckCircle2 size={20}/><div><strong>Real-data mode</strong><p>Season statistics remain at zero until you record real matches. Football OS will not manufacture performance numbers.</p></div></section></div>}
function Kpi({value,label}:{value:string;label:string}){return <div><strong>{value}</strong><span>{label}</span></div>}
function Tool({label,note,icon,tab,go}:{label:string;note:string;icon:ReactNode;tab:WorkspaceTab;go:(t:WorkspaceTab)=>void}){return <button type="button" onClick={()=>go(tab)}><span>{icon}</span><div><strong>{label}</strong><small>{note}</small></div><ArrowRight size={15}/></button>}
