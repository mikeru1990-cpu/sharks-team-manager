"use client"
import {useEffect,useState,type ReactNode} from "react"
import {ArrowRight,CalendarDays,CheckCircle2,ClipboardCheck,Dumbbell,MessageSquare,ShieldCheck,Sparkles,Trophy,Users} from "lucide-react"
import TeamContextHeader from "../layout/TeamContextHeader"
import type {WorkspaceTab} from "../../lib/workspaces"
import {getContinuingTeamTbcPlayers,leonardStanleyEvents} from "../../lib/realTeamData"
import { useSquadPlayers } from "../../lib/useSquadPlayers"
type Props={onNavigate:(tab:WorkspaceTab)=>void};type MatchState={home?:number;away?:number;seconds?:number;period?:number;running?:boolean;finished?:boolean;timeline?:unknown[]};type MatchRecord={id:number;home:number;away:number;date:string}
const continuingTbc=getContinuingTeamTbcPlayers(),nextEvent=leonardStanleyEvents[0],matchKey="football-os-matchday-workflow-v5",historyKey="football-os-match-history-v1"
export default function HomeMissionControl({onNavigate}:Props){const players=useSquadPlayers(),[match,setMatch]=useState<MatchState>({}),[history,setHistory]=useState<MatchRecord[]>([]);useEffect(()=>{try{setMatch(JSON.parse(localStorage.getItem(matchKey)??"{}"));setHistory(JSON.parse(localStorage.getItem(historyKey)??"[]"))}catch{}},[]);const hour=new Date().getHours(),greeting=hour<12?"Good morning":hour<18?"Good afternoon":"Good evening",today=new Date().toLocaleDateString("en-GB",{weekday:"long",day:"numeric",month:"long"}),hasLive=!!match.running&&!match.finished,hasStarted=(match.seconds??0)>0&&!match.finished,matchStatus=hasLive?"LIVE":hasStarted?"IN PROGRESS":"READY",matchDetail=hasLive?`${match.home??0}-${match.away??0} · ${Math.floor((match.seconds??0)/60)}' · live match running`:hasStarted?`${match.home??0}-${match.away??0} · saved on this device`:"Squad, lineup and formation ready to manage",primaryTab:WorkspaceTab=hasStarted?"matchday":"training";return <div className="fos-command-dashboard"><section className="fos-command-welcome"><div><span className="fos-command-eyebrow"><Sparkles size={13}/> COACH COMMAND</span><h1>{greeting}, Mike.</h1><p>{today} · Leonard Stanley U11 Girls</p></div><div className={`fos-command-state ${hasLive?"live":""}`}><i/><span>{hasLive?"Match live":"Team ready"}</span></div></section><TeamContextHeader currentSection="Home" nextEventLabel={hasStarted?`Matchday: ${match.home??0}-${match.away??0}`:`${nextEvent.title}: ${nextEvent.dateLabel}`}/><section className="fos-command-grid"><button className="fos-command-primary" type="button" onClick={()=>onNavigate(primaryTab)}><div className="fos-command-primary-top"><span>{hasStarted?matchStatus:"NEXT EVENT"}</span><Trophy size={20}/></div><div><small>{hasStarted?"MATCHDAY":"COMING UP"}</small><h2>{hasStarted?`Leonard Stanley ${match.home??0} – ${match.away??0} Opposition`:nextEvent.title}</h2><p>{hasStarted?matchDetail:`${nextEvent.dateLabel} · ${nextEvent.timeLabel??"TBC"}${nextEvent.location?` · ${nextEvent.location}`:""}`}</p></div><strong>{hasStarted?"Open Matchday":"Prepare session"}<ArrowRight size={17}/></strong></button><div className="fos-command-kpis"><Metric value={String(players.length)} label="Squad" note="active players" icon={<Users/>}/><Metric value={String(continuingTbc.length)} label="Review" note="squad decisions" icon={<ClipboardCheck/>}/><Metric value={String(history.length)} label="Matches" note="completed" icon={<Trophy/>}/><Metric value={hasStarted?`${match.home??0}-${match.away??0}`:"Ready"} label={hasStarted?"Score":"Status"} note={hasStarted?`${Math.floor((match.seconds??0)/60)} minutes`:"matchday setup"} icon={<ShieldCheck/>}/></div></section><section className="fos-command-section"><div className="fos-command-heading"><div><small>YOUR DAY</small><h2>What needs attention</h2></div><span>{continuingTbc.length+(hasStarted?1:2)} items</span></div><div className="fos-command-attention">{hasStarted&&<Action tone="blue" label={hasLive?"LIVE MATCH":"MATCH SAVED"} title={hasLive?"Return to the touchline":"Continue Matchday"} detail={matchDetail} cta="Open" onClick={()=>onNavigate("matchday")}/>}<Action tone="green" label="NEXT SESSION" title={nextEvent.title} detail={`${nextEvent.dateLabel} · ${nextEvent.timeLabel??"TBC"}`} cta="Plan" onClick={()=>onNavigate("training")}/>{continuingTbc.length>0&&<Action tone="amber" label="SQUAD REVIEW" title={`${continuingTbc.length} player decision${continuingTbc.length===1?"":"s"}`} detail="Review the continuing squad and confirm the correct team status." cta="Review" onClick={()=>onNavigate("players")}/>}<Action tone="navy" label="MATCHDAY" title="Lineup & match preparation" detail="Select the squad, adjust the lineup, choose the formation and prepare substitutions." cta="Prepare" onClick={()=>onNavigate("matchday")}/></div></section><section className="fos-command-section"><div className="fos-command-heading"><div><small>QUICK ACCESS</small><h2>Football tools</h2></div><span>One tap</span></div><div className="fos-command-tools"><Tool label="Matchday" note="Squad · lineup · live" icon={<Trophy/>} tab="matchday" go={onNavigate}/><Tool label="Training" note="Plan · attendance" icon={<Dumbbell/>} tab="training" go={onNavigate}/><Tool label="Players" note="Profiles · development" icon={<Users/>} tab="players" go={onNavigate}/><Tool label="Attendance" note="Take the register" icon={<ClipboardCheck/>} tab="training" go={onNavigate}/><Tool label="Calendar" note="Fixtures & events" icon={<CalendarDays/>} tab="club" go={onNavigate}/><Tool label="Parents" note="Team communication" icon={<MessageSquare/>} tab="club" go={onNavigate}/></div></section><section className="fos-command-trust"><CheckCircle2 size={19}/><div><strong>{history.length?`${history.length} real match${history.length===1?"":"es"} recorded`:"Football OS is ready"}</strong><p>{history.length?"Matchday history is connected to this dashboard.":"No manufactured performance numbers. Team information appears here as it is recorded."}</p></div></section></div>}
function Metric({value,label,note,icon}:{value:string;label:string;note:string;icon:ReactNode}){return <div className="fos-command-metric"><span>{icon}</span><strong>{value}</strong><b>{label}</b><small>{note}</small></div>}
function Action({label,title,detail,cta,onClick,tone}:{label:string;title:string;detail:string;cta:string;onClick:()=>void;tone:string}){return <button type="button" className={`fos-command-action ${tone}`} onClick={onClick}><i/><span><small>{label}</small><strong>{title}</strong><em>{detail}</em></span><b>{cta}<ArrowRight size={14}/></b></button>}
function Tool({label,note,icon,tab,go}:{label:string;note:string;icon:ReactNode;tab:WorkspaceTab;go:(t:WorkspaceTab)=>void}){return <button type="button" onClick={()=>go(tab)}><span>{icon}</span><div><strong>{label}</strong><small>{note}</small></div><ArrowRight size={15}/></button>}
