"use client"
import {useEffect,useMemo,useState} from "react"
import TeamScopeBanner from "../layout/TeamScopeBanner"
import {getActiveU11Players} from "../../lib/realTeamData"
const players=getActiveU11Players(),tabs=["Setup","Squad","Lineup","Planner","Live","Report"] as const,key="football-os-matchday-workflow-v2";type Tab=typeof tabs[number];type EventType="goal"|"sub"|"injury"|"note"|"opp-goal";type TimelineEvent={id:number;minute:number;type:EventType;label:string;offId?:string;onId?:string}
export default function MatchCentreWorkflow(){const [active,setActive]=useState<Tab>("Setup"),[selected,setSelected]=useState<string[]>(players.map(p=>p.id)),[starters,setStarters]=useState<string[]>(players.slice(0,7).map(p=>p.id)),[live,setLive]=useState<string[]>([]),[timeline,setTimeline]=useState<TimelineEvent[]>([]),[seconds,setSeconds]=useState(0),[running,setRunning]=useState(false),[home,setHome]=useState(0),[away,setAway]=useState(0),[loaded,setLoaded]=useState(false);useEffect(()=>{try{const r=localStorage.getItem(key);if(r){const s=JSON.parse(r);setSelected(s.selected??selected);setStarters(s.starters??starters);setLive(s.live??[]);setTimeline(s.timeline??[]);setSeconds(s.seconds??0);setHome(s.home??0);setAway(s.away??0)}}catch{}setLoaded(true)},[]);useEffect(()=>{if(loaded)localStorage.setItem(key,JSON.stringify({selected,starters,live,timeline,seconds,home,away}))},[loaded,selected,starters,live,timeline,seconds,home,away]);useEffect(()=>{if(!running)return;const t=window.setInterval(()=>setSeconds(v=>v+1),1000);return()=>clearInterval(t)},[running]);const minute=Math.floor(seconds/60),clock=useMemo(()=>`${String(Math.floor(seconds/60)).padStart(2,"0")}:${String(seconds%60).padStart(2,"0")}`,[seconds]),bench=selected.filter(id=>!live.includes(id));function toggleSelected(id:string){setSelected(c=>c.includes(id)?c.filter(x=>x!==id):[...c,id]);setStarters(c=>c.filter(x=>x!==id||selected.includes(id)));setLive(c=>c.filter(x=>x!==id||selected.includes(id)))}function toggleStarter(id:string){setStarters(c=>c.includes(id)?c.filter(x=>x!==id):c.length>=7?c:[...c,id])}function addEvent(type:EventType,label:string,meta?:Partial<TimelineEvent>){setTimeline(c=>[{id:Date.now(),minute,type,label:`${minute}' ${label}`,...meta},...c])}function kickoff(){if(starters.length!==7)return;setLive([...starters]);setActive("Live");setRunning(true)}function sub(offId:string,onId:string){if(!live.includes(offId)||!bench.includes(onId))return;const off=players.find(p=>p.id===offId),on=players.find(p=>p.id===onId);setLive(c=>c.map(id=>id===offId?onId:id));addEvent("sub",`${off?.name??"Player"} off → ${on?.name??"Player"} on`,{offId,onId})}return <div style={{display:"grid",gap:16,paddingBottom:140,color:"white"}}><TeamScopeBanner section="Match Centre" detail="One match state from squad selection through editable lineup, live substitutions and report."/><section style={hero}><div><small>MATCH STATUS</small><h2>{running?"LIVE":seconds?"PAUSED":"PRE-MATCH"}</h2><b>{clock}</b></div><div style={{textAlign:"right"}}><strong style={{fontSize:34}}>{home}-{away}</strong><small style={{display:"block"}}>LEONARD STANLEY</small></div></section><div style={tabsStyle}>{tabs.map(t=><button key={t} onClick={()=>setActive(t)} style={active===t?tabOn:tab}>{t}</button>)}</div>{active==="Setup"&&<section style={panel}><h2>Match setup</h2><p>{selected.length} squad · {starters.length}/7 starters</p><button style={primary} disabled={starters.length!==7} onClick={kickoff}>Kick off with this lineup</button></section>}{active==="Squad"&&<section style={panel}><h2>Squad selection</h2><p>Tap players in or out. Removing a player also removes them from the lineup.</p>{players.map(p=><button key={p.id} onClick={()=>toggleSelected(p.id)} style={row}>{selected.includes(p.id)?"✓":"+"} {p.name}</button>)}</section>}{active==="Lineup"&&<section style={panel}><h2>Starting lineup</h2><p>Select exactly 7. You can change this right up to kick-off.</p>{players.filter(p=>selected.includes(p.id)).map(p=><button key={p.id} onClick={()=>toggleStarter(p.id)} style={{...row,background:starters.includes(p.id)?"rgba(16,185,129,.22)":"rgba(2,6,23,.48)"}}>{starters.includes(p.id)?"STARTING":"BENCH"} · {p.name}</button>)}<button style={primary} disabled={starters.length!==7} onClick={kickoff}>Save lineup & kick off</button></section>}{active==="Planner"&&<section style={panel}><h2>Match planner</h2><p>Starting seven: {players.filter(p=>starters.includes(p.id)).map(p=>p.name).join(", ")||"Not complete"}</p><p>Bench: {players.filter(p=>selected.includes(p.id)&&!starters.includes(p.id)).map(p=>p.name).join(", ")||"None"}</p></section>}{active==="Live"&&<Live clock={clock} running={running} setRunning={setRunning} home={home} away={away} setHome={setHome} setAway={setAway} live={live} bench={bench} sub={sub} addEvent={addEvent}/>} {active==="Report"&&<section style={panel}><h2>Match report</h2><p>{home}-{away} · {clock} · {timeline.length} events</p>{timeline.length?timeline.map(e=><div key={e.id} style={event}>{e.label}</div>):<div style={event}>No events yet.</div>}</section>}</div>}
function Live({clock,running,setRunning,home,away,setHome,setAway,live,bench,sub,addEvent}:{clock:string;running:boolean;setRunning:(v:boolean)=>void;home:number;away:number;setHome:React.Dispatch<React.SetStateAction<number>>;setAway:React.Dispatch<React.SetStateAction<number>>;live:string[];bench:string[];sub:(a:string,b:string)=>void;addEvent:(t:EventType,l:string)=>void}){const [off,setOff]=useState("");return <section style={panel}><div style={liveHead}><div><small>LIVE MATCH</small><h2 style={{fontSize:32,margin:"4px 0"}}>{clock}</h2></div><strong style={{fontSize:32}}>{home}-{away}</strong></div><div style={three}><button style={primary} onClick={()=>setRunning(!running)}>{running?"Pause":"Start"}</button><button style={action} onClick={()=>{setHome(v=>v+1);addEvent("goal","Leonard Stanley goal")}}>⚽ Goal</button><button style={action} onClick={()=>{setAway(v=>v+1);addEvent("opp-goal","Opposition goal")}}>Opp goal</button></div><h3>On pitch</h3>{live.map(id=>{const p=players.find(x=>x.id===id);return <button key={id} style={{...row,background:off===id?"rgba(239,68,68,.2)":"rgba(2,6,23,.48)"}} onClick={()=>setOff(off===id?"":id)}>{off===id?"SELECTED OFF · ":"ON · "}{p?.name}</button>})}<h3>Bench {off?"· choose player ON":"· select a player OFF first"}</h3>{bench.map(id=>{const p=players.find(x=>x.id===id);return <button disabled={!off} key={id} style={row} onClick={()=>{sub(off,id);setOff("")}}>↔ {p?.name}</button>})}</section>}
const hero={borderRadius:22,padding:16,background:"#0d1829",border:"1px solid #1f3048",display:"flex",justifyContent:"space-between",alignItems:"center"},panel={borderRadius:22,padding:16,background:"#0d1829",border:"1px solid #1f3048",display:"grid",gap:9},tabsStyle={display:"grid",gridTemplateColumns:"repeat(6,1fr)",gap:4},tab={minHeight:43,border:0,borderRadius:11,background:"#111c2e",color:"#718096",fontSize:10,fontWeight:900},tabOn={...tab,background:"#2563eb",color:"white"},row={width:"100%",minHeight:48,border:"1px solid #24344c",borderRadius:13,padding:"10px 12px",background:"rgba(2,6,23,.48)",color:"white",textAlign:"left" as const,fontWeight:850},primary={minHeight:50,border:0,borderRadius:13,background:"#2563eb",color:"white",fontWeight:950},action={...primary,background:"#16243a"},three={display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:7},liveHead={display:"flex",justifyContent:"space-between",alignItems:"center"},event={padding:11,borderRadius:12,background:"rgba(2,6,23,.45)",color:"#cbd5e1"}
